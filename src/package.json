{
  "name": "backpack",
  "module": "server/server.ts",
  "type": "module",
  "scripts": {
    "start": "bun run server/server.ts",
    "dev": "bun run server/server.ts --watch"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "hono": "^4.5.11"
  }
}